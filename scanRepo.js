
module.exports = function scanRepo (repoPath, {from, to} = {}) {
  // Check if "repoPath" is actually a repository

  // Loop through "from" to "to"
  // - Checkout
  // - Check directory
  // Repeat
}
